'use strict';

/**
* AngularJS service for storing the retrieved article instance.
*
* @class article
*/
angular.module('authoringEnvironmentApp').service('article', [
    function () {
        // this is just a container for article instance and modified flag
        // (might be refactored, this does not "feel" like the most
        //  elegant solution...)
    }
]);
